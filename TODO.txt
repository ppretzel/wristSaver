Alle geparsten Hotkeys in eine große liste und die am Anfang ausgeben, damit man überprüfen kann obs stimmt und perspektivisch auch daraus eine Hilfe-Anzeige generieren kann
oder daraus dann auch ein Overlay mit allen verfügbaren Tastenkürzeln für das aktuelle Fenster/die Ausgabe

In ishChangeSelection weiter die jeweilige Station schon auswählen lassen

auch WaitForImage(checkInterval := 500, timeout := 0) (oder so) und MoveToImage schreiben

Warum funktioniert es zB auf dem mittleren Rechner in Sams Büro nicht??